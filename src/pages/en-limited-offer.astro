<style>
  :global(html),
  :global(body) {
    margin: 0;
    padding: 0;
    width: 100%;
    background: #050505;
    overflow-x: hidden;
  }

  :global(body) {
    color: #ffffff;
    font-family: Arial, Helvetica, sans-serif;
  }

  * {
    box-sizing: border-box;
  }
  .lang-btn {
    position: absolute;
    top: 22px;
    right: 22px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 10px 14px;

    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.12);

    background: rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(8px);

    color: rgba(255, 255, 255, 0.78);
    text-decoration: none;

    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.02em;

    transition:
      background 0.2s ease,
      border-color 0.2s ease,
      color 0.2s ease;
  }

  .lang-btn:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(239, 68, 68, 0.35);
    color: #ffffff;
  }
  .page {
    width: 100%;
    margin: 0;
    padding: 0;
    background: #050505;
  }

  .flyer {
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
    overflow: hidden;
    background: #050505;
  }

  .hero {
    position: relative;
    padding: 56px;
    background:
      radial-gradient(
        circle at top right,
        rgba(239, 68, 68, 0.28),
        transparent 35%
      ),
      radial-gradient(
        circle at bottom left,
        rgba(239, 68, 68, 0.16),
        transparent 35%
      );
  }

  .grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 42px;
    align-items: center;
  }

  .eyebrow {
    color: #f87171;
    text-transform: uppercase;
    letter-spacing: 0.28em;
    font-size: 13px;
    font-weight: 800;
    margin-bottom: 22px;
  }

  h1 {
    font-size: clamp(48px, 8vw, 82px);
    line-height: 0.9;
    letter-spacing: -0.06em;
    font-weight: 950;
    margin: 0 0 26px;
  }

  .red {
    color: #ef4444;
  }

  .description {
    color: #d4d4d8;
    font-size: 18px;
    line-height: 1.55;
    margin-bottom: 30px;
    max-width: 560px;
  }

  .reward {
    display: inline-flex;
    align-items: center;
    gap: 16px;
    background: #dc2626;
    padding: 18px 24px;
    border-radius: 22px;
    box-shadow: 0 18px 40px rgba(220, 38, 38, 0.35);
  }

  .reward-number {
    font-size: 52px;
    font-weight: 950;
    line-height: 1;
  }

  .reward-title {
    font-size: 19px;
    font-weight: 900;
  }

  .reward-small {
    color: #fee2e2;
    font-size: 14px;
    margin-top: 4px;
  }

  .card {
    background: rgba(24, 24, 27, 0.86);
    border: 1px solid #3f3f46;
    border-radius: 28px;
    padding: 32px;
    box-shadow: 0 18px 60px rgba(0, 0, 0, 0.45);
  }

  .card h2 {
    font-size: 34px;
    margin-bottom: 22px;
    letter-spacing: -0.04em;
  }

  .benefit {
    display: flex;
    gap: 14px;
    align-items: flex-start;
    background: rgba(39, 39, 42, 0.82);
    border: 1px solid #3f3f46;
    padding: 15px;
    border-radius: 18px;
    margin-bottom: 12px;
    color: #e4e4e7;
    font-size: 17px;
    line-height: 1.25;
  }

  .dot {
    width: 10px;
    height: 10px;
    min-width: 10px;
    border-radius: 50%;
    background: #ef4444;
    margin-top: 6px;
  }

  .cta {
    background: #dc2626;
    padding: 34px 56px;
    display: flex;
    gap: 28px;
    align-items: center;
    justify-content: space-between;
  }

  .cta h3 {
    font-size: 31px;
    font-weight: 950;
    margin-bottom: 8px;
    letter-spacing: -0.04em;
  }

  .cta p {
    color: #fee2e2;
    font-size: 17px;
    line-height: 1.45;
    max-width: 560px;
  }

  .contact {
    background: #ffffff;
    color: #050505;
    border-radius: 22px;
    padding: 24px 28px;
    min-width: 260px;
    text-align: center;
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.28);
  }

  .contact-label {
    color: #71717a;
    text-transform: uppercase;
    letter-spacing: 0.24em;
    font-size: 12px;
    font-weight: 800;
    margin-bottom: 10px;
  }

  .contact-name {
    font-size: 25px;
    font-weight: 950;
    margin-bottom: 4px;
  }

  .contact-small {
    color: #71717a;
    font-size: 14px;
    margin-top: 10px;
  }

  @media (max-width: 950px) {
    .page {
      background: #050505;
    }

    .flyer {
      width: 100%;
      max-width: none;
      margin: 0;
    }

    .hero {
      padding: 32px 22px;
    }

    .grid {
      display: flex;
      flex-direction: column;
    }

    h1 {
      font-size: clamp(54px, 17vw, 76px);
      line-height: 0.87;
    }

    .description {
      font-size: 18px;
      line-height: 1.55;
    }

    .reward {
      width: 100%;
    }

    .card {
      padding: 24px;
    }

    .cta {
      padding: 32px 22px;
      flex-direction: column;
      align-items: stretch;
    }

    .contact {
      width: 100%;
      min-width: 0;
    }
    .lang-btn {
      top: 14px;
      right: 14px;

      padding: 8px 12px;

      font-size: 11px;
      letter-spacing: 0.02em;

      border-radius: 999px;

      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);

      backdrop-filter: blur(8px);
    }
  }
</style>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<div class="page">
  <main class="flyer">
    <section class="hero">
      <div class="grid">
        <div>
          <a class="lang-btn" href="/es-limited-offer">
            Ver en espa&ntilde;ol
          </a>

          <p class="eyebrow">Mobile Martial Arts Coaching</p>

          <h1>
            REFER A<br />
            FRIEND.<br />
            EARN <span class="red">FREE<br />CLASSES.</span>
          </h1>

          <p class="description">
            We bring martial arts directly to your home with private,
            personalized instruction focused on confidence, discipline, fitness,
            and real self-defense.
          </p>

          <div class="reward">
            <div class="reward-number">2</div>
            <div>
              <div class="reward-title">FREE PRIVATE CLASSES</div>
              <div class="reward-small">
                when your referral joins a 4-class package
              </div>
            </div>
          </div>
        </div>

        <div class="card">
          <h2>Why Families Love It</h2>

          <div class="benefit">
            <span class="dot"></span>
            <span>Private in-home lessons</span>
          </div>

          <div class="benefit">
            <span class="dot"></span>
            <span>Flexible scheduling</span>
          </div>

          <div class="benefit">
            <span class="dot"></span>
            <span>Kids & adults welcome</span>
          </div>

          <div class="benefit">
            <span class="dot"></span>
            <span>Karate - Kickboxing - MMA fundamentals</span>
          </div>

          <div class="benefit">
            <span class="dot"></span>
            <span>Confidence & discipline building</span>
          </div>

          <div class="benefit">
            <span class="dot"></span>
            <span>15+ years martial arts experience</span>
          </div>
        </div>
      </div>
    </section>

    <section class="cta">
      <div>
        <h3>WE GROW THROUGH REFERRALS</h3>

        <p>
          If you know someone interested in martial arts training, fitness,
          confidence, or self-defense - send them my way.
        </p>
      </div>

      <div class="contact">
        <div class="contact-label">Contact</div>

        <div class="contact-name">Brandon Mederos</div>

        <div>Mobile Private Lessons</div>

        <div class="contact-small">
          (786) 531-7439 <br />
          IG: @brandongerardomederos
        </div>
      </div>
    </section>
  </main>
</div>
